%nav#user
  %ul
    - if current_user
      %li= "Logged in as #{current_user.name}"
      %li= link_to "Logout", logout_path, method: :delete, id: 'logout_link'
    - else
      %li= link_to 'Signup', signup_path, id: 'signup_link'
      %li or
      %li= link_to 'Login', login_path
