= form_for [@userstory, @acceptance_test], remote: true do |f|
  = f.label :description
  = f.text_area :description
  .field
    = f.label :complete
    = f.check_box :complete
  = f.submit 'Save'

-#= link_to 'Back', userstory_acceptance_tests_path(@userstory), id: 'back_link'
