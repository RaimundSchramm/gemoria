- content_for :nav_breadcrumbs do
  %ul
    - if current_user
      %li= link_to 'Dashboard', dashboard_path
      %li= link_to "#{@project.name}", @project
      %li Manage userstories

%section#userstories.group
  %header
    %h1= "Manage userstories"
  = render 'menue'
  = render 'current_sprint'
  = render 'backlog'

= render 'done'
